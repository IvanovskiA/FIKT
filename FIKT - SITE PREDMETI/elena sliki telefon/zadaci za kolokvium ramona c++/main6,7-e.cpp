//Elena Desanoska INKI-767

#include <iostream>
#include<math.h>
using namespace std;

int main;
   {
   const double e=2.71;
   const double a=6;
   double x, y;
   std::cout<<"Vnesi 2 vrednosti: "<<std::end1;
   std::cin >>x >> y;
   double rezultat= (cbrt((sqrt((pow(e,(cos(x)))+(pow(e,(-sin(y))))))/((pow(a,2))+(x/y)*(sqrt(log(2*a*x*y)))))));
   std::cout<<std::end1;
   std::cout <<"Rezultat: "<<rezultat;
   return 0;
}
